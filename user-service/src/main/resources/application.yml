spring:
  application:
    name: user-service

server:
  port: 8082
  servlet:
      context-path: /user-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
