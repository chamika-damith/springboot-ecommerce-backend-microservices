spring:
  application:
    name: order-service

server:
  port: 8083
  servlet:
      context-path: /order-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
